# Findings index — AUTO-GENERATED by tools/findings.py (do not hand-edit).

Grep this (or `tools/findings.py <words>`) BEFORE investigating a bug — to find whether it's
already solved or ruled out. 195 findings across 10 subsystems.


## ai

- **ActorMeleeEngage::doIt — scratchpad 0x1F80009C stale-write on the "arm-directly" path (RESOLVED, 2026-07-10)** [?] — two independent frontier-convergence agents (2026-07-10) reproduced an SBS-full  ↪ docs/findings/ai.md
- **Combat-cluster autonav coverage gap — CLOSED for 2/3 addresses; combat-leg SBS divergence RESOLVED (2026-07-10)** [?] —   ↪ docs/findings/ai.md

## animation

- **FIXED: `anim_unpack_pose_triple` ate a shared nibble, corrupting every subsequent per-limb pose field (2026-07-08)** [?] —   ↪ docs/findings/animation.md
- **INVESTIGATED, REVERTED: `Animation::step` (FUN_80076D68) wiring — frame mirror is correct, but exposes a pre-existing `anim_vm_76d68` fidelity gap for a pool-adjacent node address (2026-07-08)** [?] —   ↪ docs/findings/animation.md
- **RESOLVED: ActorTomba::frameTick (FUN_8005950C) intra-body register-faithfulness — C++ locals are not enough when a substrate callee spills the callee-saved reg (2026-07-09)** [?] —   ↪ docs/findings/animation.md
- **RESOLVED: Cull camera-relative wrapper family (0x8007778C/800777FC/80077ACC/800779D0/80077A4C/800778E4) wired byte-exact via guest-frame mirroring (2026-07-08)** [?] —   ↪ docs/findings/animation.md
- **RESOLVED: turnBiasCompute/outerTransitionGate/outerTransitionCommit/assetReady — §9 promotion found a MIPS branch-delay-slot misread + 6 missing r31 mirrors + 1 wrong-constant gate (2026-07-10)** [?] —   ↪ docs/findings/animation.md
- **REVERTED (attempted, does not apply): `Animation::attach` (FUN_80077C40) guest-frame mirror — no single guest call site to mirror against (2026-07-08)** [?] —   ↪ docs/findings/animation.md
- **SUPERSEDED 2026-07-08 (later same day): the "no single guest call site" premise below was WRONG — attach's frame IS now mirrored, `isDeadStackScratch` DELETED** [?] —   ↪ docs/findings/animation.md

## audio

- **FUN_80075824 was WRONGLY wired to MusicCoord::musicFadeIn — real fn = per-voice volume mixer tick (2026-07-03)** [FIXED — `MusicCoord::voiceMixTick(uint32_t voice_base)` ported (game/audio/music_coord.cpp] —   ↪ docs/findings/audio.md
- **Ownership sweep of 0x800A0000-0x800BFFFF found ZERO owned/ownable dispatch-target code — the whole band is DATA (2026-07-08)** [no code ported this session (nothing legitimate to port). Do NOT re-run the recdep hunt on] —   ↪ docs/findings/audio.md
- **Sequencer (libsnd SsSeqCalled) cluster wiring — 5 bugs found at §9 re-verify; 8 addresses wired 0-diff, 5 left unwired-honest (2026-07-10)** [?] —   ↪ docs/findings/audio.md
- **`channelNoteInit` (0x80091970) flagged by the new generalized `MIRROR_VERIFY=all` gate — OPEN, needs re-check (2026-07-10)** [?] —   ↪ docs/findings/audio.md
- **pc_skip vs oracle: SPU register stream divergences — MODE=skip (2026-07-10, OPEN)** [?] —   ↪ docs/findings/audio.md
- **pc_skip vs oracle: SPU register stream divergences — MODE=skip (2026-07-10, root-caused)** [?] —   ↪ docs/findings/audio.md

## camera

- **Oracle recompiler gap: yFloor (0x8006C80C) render-mode 1 target 0x8006C844 not discovered** [known-issue (oracle/recompiler function-discovery gap; native `CutsceneCamera::yFloor` handles it).] — the camera oracle unit test (PSXPORT_SELFTEST=camera) skips ~188/3000 yFloor iterations;  ↪ docs/findings/camera.md
- **Superseded earlier conclusions (kept so the dead ends aren't re-walked)** [?] —   ↪ docs/findings/camera.md
- **✅ DONE: camera DISPATCHER owned natively — `CutsceneCamera::update()` + `::init()` (later-294)** [?] —   ↪ docs/findings/camera.md
- **✅ DONE: driver modes 2/3/4 owned — snap-follow variants (later-294b)** [?] —   ↪ docs/findings/camera.md
- **✅ DONE: init() fully native — initPlace (0x8006E918) + initSeedGrp (0x8006CBA8) (later-294d)** [?] —   ↪ docs/findings/camera.md
- **✅ DONE: post-mode TAIL owned — `shakeTail()`, the camera SHAKE state machine (later-294e)** [?] —   ↪ docs/findings/camera.md
- **✅ DONE: scripted LOOK-ANGLE builder unit owned — follow tree now fully native (later-294c)** [?] —   ↪ docs/findings/camera.md
- **✅ RESOLVED: the free-roam camera IS the resident camera (snapFollow→lookat), already owned + verified** [resolved (2026-07-01, later-293). The free-roam field camera IS the resident camera.] — across later-290/291/292 I repeatedly concluded the resident MAIN.EXE camera "never runs in  ↪ docs/findings/camera.md

## fmv-cd

- **CdSync (FUN_8008a6ec) is already HLE'd to report completion** [known-good (do not re-investigate)] — worry that a `do { FUN_80089e1c(9,0,0)=CdControlB(CdlPause) } while(==0)` teardown loop (or any CdControlB-blocking) will spin forever under the no-IRQ runtime.  ↪ docs/findings/fmv-cd.md
- **Native port of libcd directory cache — CdNewMedia + CdCacheFile (2026-07-04)** [fixed — libcd cache region f0 zero-diff; remaining 522 B at f0 are pre-existing Slip #6 (task-fiber cadence), unrelated.] — SBS `MODE=gameplay` (pc_skip=true vs recomp) f0 divergence 1273 B — 8 B at `0x800AC2D4..0x800AC2DC` (libcd cached-dir-idx + media cookie), ~500 B at `0x80102D68+` (path-table cache), ~500 B at `0x80102768+` (file-entry cache for last-cached dir), plus 2 KB of dir-sector scratch at `0x80104368+`.  ↪ docs/findings/fmv-cd.md
- **SBS both-mode freezes on the intro OP.STR movie** [fixed 5a61c3d] — `PSXPORT_SBS_MODE=both ./run.sh` freezes after `[fmv] MOVIE/OP.STR -> LBA 152238`, spamming the game's own `time out in strNext()` (seconds apart); window stops updating. PSXPORT_NO_FMV=1 does NOT help.  ↪ docs/findings/fmv-cd.md

## render

- **"Upside-down Tomba" in hooked-fish beat — FALSE ALARM + eprojv tool (2026-07-14, bug #44 closed)** [?] —   ↪ docs/findings/render.md
- **#2b New Game -> DEMO s48=3 is the s3 MENU (not the SOP narration) — build-ready** [fixed 2026-07-16 (s3MenuNative, data-driven; s3 pc-vs-psx RMSE 0.000)] — selecting New Game on the title crashes pc_render `unimplemented native rendering ... stage=0x801062E4 sm[0x48]=3 overlay_sig=0x3C021F80`; user "cursor after New Game doesn't work / Circle goes back"  ↪ docs/findings/render.md
- **#5 SOP narration void beat renders ~black under pc_render — vortex object not drawing** [fixed 2026-07-16 (Render::narrationSwirlRender — native swirl producer; void beat 2.7%->59.1% coverage vs ref 58.9%, RMSE 45->20 = within the accepted native-3D band (field itself shows 61))] — after New Game, the SOP intro (GAME stage, overlay 0x3C021F80) shows a mostly-black screen  ↪ docs/findings/render.md
- **0x8003F9A8 474-prim attribution resolved — 4 substrate list-walkers, no hidden emitter (2026-07-14)** [all 4 walkers (+ the FUN_8003BED8 shared-tail split of BCF4) ported as `Render::] —   ↪ docs/findings/render.md
- **0x800C0000-0x800C8FFF "massive divergence" — FALSE POSITIVE; it's the GPU packet pool (2026-07-08)** [?] —   ↪ docs/findings/render.md
- **0x8013CDD4 port — ambiguity SETTLED (2026-07-14): load-bearing field reuse, port unblocked** [SBS-full AUTONAV **0-diff to f26790+** (895 checkpoints, exits on 250s wall-clock] —   ↪ docs/findings/render.md
- **0x8013DD48 is a GTE cull/midpoint leaf (sibling of the already-excluded 0x8013DD34) — LEAVE PSX** [RE'd, then EXCLUDED — this is a render/GTE hardware leaf, not game logic. Do not port.] —   ↪ docs/findings/render.md
- **2D-poly overlays and world-billboard sprites on the field 2D-only walk (open frontier)** [known-issue (frontier)] — in-field gradient/fade PANELS (polys) or world-billboard sprites may be missing or flat in the 2D-only field overlay pass.  ↪ docs/findings/render.md
- **BbRec capture sources = live CRs, not fixed scratch (C788-class rotation bug) (2026-07-16)** [?] —   ↪ docs/findings/render.md
- **Billboard OBJECT MODEL — manager node + per-particle records (RE 2026-07-10, operator)** [?] — gems "react poorly" at 60fps; historic objid rectangle covered ALL visible billboards  ↪ docs/findings/render.md
- **CORRECTION (USER, 2026-07-10): throw-chain is a RENDERER bug, not a pc_skip fork** [?] —   ↪ docs/findings/render.md
- **Coplanar z-FIGHTING on barrel/decoration surfaces (black chunks flicker) — paint-order depth tiebreak (2026-07-10, PARTIAL FIX)** [?] —   ↪ docs/findings/render.md
- **Double-submission (#48) FIXED (2026-07-15) — scene-native-owned meshes dropped from the OT walk** [?] —   ↪ docs/findings/render.md
- **FUN_80039F4C text-label renderer OWNED (Render::textLabelEmit) + BbRec lerp removed (2026-07-16, #67 RE work cont.)** [?] —   ↪ docs/findings/render.md
- **FUN_800518FC three-way split → NodeXform::buildWithOffset sole owner (2026-07-15)** [?] —   ↪ docs/findings/render.md
- **Fisherman's-hut interior "much different than oracle" (2026-07-10, OPEN — repro UNBLOCKED 2026-07-14)** [the interior divergence could **NOT be reproduced headless** this session, for two concrete] —   ↪ docs/findings/render.md
- **Free-roam recomp-MISS: `jal 0x80109450` with A00 resident — FIXED (later-286): recompiler fall-through-into-fragment guest-sp leak** [✅ FIXED (later-286, commit pending). `newgame; run 6000` now holds free-roam steady (t0 st=2 s48=2) to the end with ZERO net sp leak and no miss. **BOTH prior diagnoses were WRONG:** later-284c blamed the A00 render recursion (falsified by later-285); later-285 then blamed a "gameplay object-graph recursion ~4× deeper than the oracle" (ALSO WRONG — it is not a recursion-DEPTH divergence at all).] — after the later-284b freeze fix, `newgame; run` plays narration→cliff→free-roam field, then aborts ~53 frames into free-roam (≈f1184, no input) at `[recomp-MISS] no recompiled fn for 0x80109450 (caller ra=0x801088B0)`.  ↪ docs/findings/render.md
- **GATING STANDARD — MIRROR_VERIFY is NOT SBS-full (2026-07-15, regression lesson)** [?] —   ↪ docs/findings/render.md
- **GPU-DMA Enqueue residual CLOSED — LoadImage streamer (0x80082734) wired + register-liveness bug fixed (2026-07-10)** [?] —   ↪ docs/findings/render.md
- **Graphical enhancements rewired PC-native / read-only overlay (2026-07-08)** [?] —   ↪ docs/findings/render.md
- **High-res (ires) looks blurry — it's supersample-to-native, not present-at-high-res** [fixed 2026-07-16 (unified present-at-high-res; verify on-window by USER eyeball)] — enabling high-res (internal-resolution / ires mod) makes the image look BLURRIER/softer than  ↪ docs/findings/render.md
- **Hut interior FIXED (2026-07-15) — authored OT sub-scene walked full, not native-field-reconstructed** [?] —   ↪ docs/findings/render.md
- **Intro-cutscene FREEZE + red-diagonal render corruption over idle frames — render walk overflows task0 stack into the task table** [FIXED (later-284b). Removed the redundant PSX-render-underneath; `dv_restore_pre` alone keeps guest state correct. Cutscene now plays through (narration → cliff → free-roam field), no freeze, no red corruption. VERIFIED: oraclediff convergent through free-roam onset; screenshots fx_700/1000/1145 render clean. Exposed the NEXT frontier: `jal 0x80109450` recomp-MISS in free-roam (A00-resident MODE slot has a jump-table there, not a fn — see the sibling finding below).] — after `newgame` (REPL) the opening runs but Tomba HANGS in the caught-on-fishing-line pose and never reaches the fisherman/house-fire dialog no matter how long you idle (`run`); at ~f3705 (frame counter, ≈f3737) the whole screen degrades into DARK-RED DIAGONAL garbage that grows worse each frame. Field STATE looks unchanged (scene byte 0x800bf9b4=0, MODE overlay 0x80109450=0x801138A4, stage GAME). Circle does NOT advance it (there is no dialog up — it's frozen BEFORE the dialog); only Start-`skip` (pulsing Start) forces past it.  ↪ docs/findings/render.md
- **Intro-narration cutscene rendered wrong (void = sea, cliff banded) — native field path hijacked the SOP scenes** [fixed (later-281) — verified vs the software-GPU oracle (void = purple swirl + Tomba + text; cliff = grassy cliff + clean sea), free-roam unchanged, gates green] — the intro NARRATION (after New Game): the dark "void" beat ('Was she kidnapped?') drew the SEA + characters instead of a black void with a swirl EFFECT; the cliff beat's water looked banded/striped  ↪ docs/findings/render.md
- **Narration-end -> fisherman-cutscene LOADING SCREEN shows garbage (pc_render) instead of black-hold + "Loading....." (2026-07-08)** [?] —   ↪ docs/findings/render.md
- **Native-but-UNREGISTERED leaves → substrate fallthrough (2026-07-15) — native-ization candidates** [?] —   ↪ docs/findings/render.md
- **Opening-cutscene narration renders nothing on the native FIELD path** [fixed 10a07e0] — New Game → the opening story cutscene ("Tomba is living peacefully in the country when Zippo finds a mysterious...") draws NOTHING; the prior menu's stale VRAM shows through (looks like the menu "overstays"/garbles).  ↪ docs/findings/render.md
- **Owned the per-object cmd-list dispatch chain 0x8003CDD8/0x8003F698 (2026-07-08)** [?] —   ↪ docs/findings/render.md
- **PSX render path always executes underneath; pc_render display pass is READ-ONLY (2026-07-07, issue #32)** [?] —   ↪ docs/findings/render.md
- **Phase-3 fallthrough native-ization — animTick/walkStart wired + gated (2026-07-15)** [?] —   ↪ docs/findings/render.md
- **Pre-existing render-port port_check FAILs (byte-divergence, deferred) [2026-07-17]** [KNOWN, deferred (render path). Surfaced by `tools/port_check.py --all`.] —   ↪ docs/findings/render.md
- **PutDrawEnv cluster + Font::drawText/glyphEmit + Str::length — wiring pass (2026-07-10)** [WIRED. All 9 addresses installed via `engine_set_override_main` (oracle-gated thunk,] — frontier wiring session — promote the banked PutDrawEnv chain (0x800815D0 + 4 leaf  ↪ docs/findings/render.md
- **Quad batching (#45) — evidence pass: scope narrowed to 0x80039F4C (2026-07-14)** [?] —   ↪ docs/findings/render.md
- **Screen-fade transitions: SSAO/dynamic-shadows are dead stubs; FUN_8007E9C8 (fade-rect builder) only PARTIALLY native-owned** [(1)/(2) NOT YET diagnosed past ruling out SSAO/light (see below) — needs non-visual RE, not] —   ↪ docs/findings/render.md
- **ScreenFade held-latch permanent black on default `./run.sh` free-roam (2026-07-08)** [?] —   ↪ docs/findings/render.md
- **Un-owned FUN_8007E9C8 fade callers — 3 of 3 SHIPPED (2026-07-03)** [ALL 3 native (last landing: commit 560bac0). Also solved the sibling arc — the cutscene-script INTERPRETER that dispatches the A06 pair is fully native (dd40602 + 4c331a3 + a70092a), so op-0x03E fnptr routing is live for any A06 fade fn registered as a `beh_*`.] —   ↪ docs/findings/render.md
- **Weapon "ball but no CHAIN" (#28 follow-up) — chain-LINK billboards already render; only a minor grab/dust semi-quad is dropped (2026-07-10, could-not-reproduce a significant chain)** [?] —   ↪ docs/findings/render.md
- **Widescreen re-enabled (aspect FOV + present) — SDL_GPU Pass-1 lost the wide present + OFX (2026-07-10)** [FIXED (headless field shots at 4:3 / 16:9 / 21:9: single, centered, genuinely-wider,] —   ↪ docs/findings/render.md
- **Widescreen: native 2D centered via ONE queue layout authority + emitter culls widened (2026-07-16, USER request)** [?] —   ↪ docs/findings/render.md
- **Z-fight sweep 2026-07-14 — barrel fix HOLDS; new double-submission class; 2 zfight tooling defects** [?] —   ↪ docs/findings/render.md
- **`NodeXform`'s 6 methods missing guest-stack-frame mirror — reproducible f117-class SBS residual (2026-07-08)** [?] — after owning `Render::perObjRenderDispatch`/`billboardCompose1`/`billboardCompose2`/  ↪ docs/findings/render.md
- **`PSXPORT_MIRROR_VERIFY=all` generalization — reproduces the tracked register-faithfulness/** [?] —   ↪ docs/findings/render.md
- **billboardEmit particles now a DISPLAY-PASS producer — dual-emit deleted, true fps60 interpolation (2026-07-16, #67 RE work)** [?] —   ↪ docs/findings/render.md
- **codemap --conflicts FALSE POSITIVE — FUN_8002AB5C (terrain display pass) (2026-07-15)** [?] —   ↪ docs/findings/render.md
- **f179 task-0-stack `0x801FE924` — `OverlayGroundGt3Gt4::gt3/gt4` mode==2 SZ-mirror used the WRONG stack offset (2026-07-10, RESOLVED)** [?] — SBS-full frontier moved to f179 after the packet-pool write-order fix (commit 79f50a5)  ↪ docs/findings/render.md
- **fieldObjectsRender TYPE-CORRECT routing — pre-composed-matrix classes no longer double-camera'd (2026-07-16)** [?] —   ↪ docs/findings/render.md
- **fps60 2D flicker (#67) — blanket "tier1 owns RQ_WORLD" dropped guest-time drawables from interp presents (2026-07-16, RESOLVED)** [?] —   ↪ docs/findings/render.md
- **fps60 HUT-INTERIOR FLICKER — interpolated frames show the field exterior (2026-07-15, USER inspect)** [?] —   ↪ docs/findings/render.md
- **fps60 billboard anchor must be PER-PARTICLE, not per-manager-node (2026-07-10, gems RESOLVED)** [?] —   ↪ docs/findings/render.md
- **fps60 redesigned as TRUE per-object interpolation (2026-07-10, RESOLVED)** [?] —   ↪ docs/findings/render.md
- **ires (internal resolution) 2D/HUD blur (bug #55) — band split + coverage gate (2026-07-15, PARTIAL FIX)** [?] —   ↪ docs/findings/render.md
- **ires (internal resolution) modifier is a NO-OP — never wired past the readout (2026-07-14)** [?] —   ↪ docs/findings/render.md
- **libgpu GPU-DMA completion-queue cluster + DrawSync/ClearOTagR — wide-RE wiring pass (2026-07-10)** [?] —   ↪ docs/findings/render.md
- **native field path renders ONLY the sky/sea backdrop — the 3D world (grass/house/tree/Tomba) is occluded** [FIXED 2026-06-26 (texpage-provenance backdrop classification) — was the later-235..245 "sea on top" / render-ordering blocker, OPEN #1] — in the GAME free-roam seaside field, the native render path (`ov_scene_native`, field-default in `ov_draw_otag`) shows the cyan sea + sky backdrop + 2D HUD but NO 3D world; `PSXPORT_RENDER_PSX=1` (PSX OT walk) renders the full correct scene (grass/house/tree/fence/crane/Tomba). Same on SDL_GPU and the old VK renderer (USER-confirmed).  ↪ docs/findings/render.md
- **overlay_ground_gt3gt4 cluster (8013FB88/8013FE58/801401B8) — depth >>2 + range gate (2026-07-09, packet-pool RESOLVED; f118/f62 register-faithfulness RESOLVED 2026-07-10; f179 task-0-stack mode==2 mirror-offset RESOLVED 2026-07-10 — see bottom)** [?] —   ↪ docs/findings/render.md
- **packet-pool 39-record shortfall at f118 — RETRACTED, false positive (2026-07-10)** [dead-end (tool false-positive, not a game-state bug) — 2026-07-10.] — `PSXPORT_SBS_PREWATCH=0x800BF544 PSXPORT_SBS_WW_ONVALUEDIVERGE=1` reports  ↪ docs/findings/render.md
- **perobj_billboard cluster (C2D4/C464/C8F4) — BUF base + register-faithfulness (2026-07-09, RESOLVED)** [?] —   ↪ docs/findings/render.md
- **stack-depth cluster mechanically, plus surfaces new OPEN addresses in the same chain (2026-07-10)** [?] —   ↪ docs/findings/render.md
- **submitQuad quads (flame/rope classes) now display-pass — composed-CR camera factorization (2026-07-16, #67 RE work cont.)** [?] —   ↪ docs/findings/render.md

## sbs

- **(historical) the symptom investigation that led to later-272** [SUPERSEDED by the FIXED block above — kept for the diagnostic trail.] —   ↪ docs/findings/sbs.md
- **+6 alloc delta at 0x800ED098 FIXED — case-swap bug in native `beh_anim_trigger_gates` STATE 0 (`v1 == 2` should be `v1 == 3`); post-fix net=+0, f240 ramdiff 4794 → 2074 B (−55%)** [?] —   ↪ docs/findings/sbs.md
- **3-slot 0x800ED098 delta ISOLATED to Sop::fieldMode case 0 spawn loop — new `PSXPORT_SBS_ALLOCTRACE=1` per-frame allocator counter landed** [?] —   ↪ docs/findings/sbs.md
- **3-slot 0x800ED098 delta caller **FALSIFIED** — flagbit_timer_machine STATE 0 fires 2× on BOTH cores (recomp path on B verified); +3 net source still unattributed** [?] —   ↪ docs/findings/sbs.md
- **AUTONAV: reaching real player control (s4e==9 -> 1); interactive play is A/B byte-identical (2026-07-08)** [RESOLVED. AUTONAV now reaches real player control; a full interactive walk/jump run is] — SBS AUTONAV (and AUTO_SKIP) stopped at the intro-cutscene end (cutscene flag  ↪ docs/findings/sbs.md
- **FIX case-swap ONE-SIDED byte 0x800EE0DE — off-by-0x10000 tbl constant in native `beh_prng_velocity_machine` STATE-0/STATE-2 → wrong geom-block table, wrong animation cmd list, +0xe timer never advances** [?] —   ↪ docs/findings/sbs.md
- **FULL-mode 2-frame A→B lead ROOT-CAUSED: native DEMO/GAME dispatcher runs substates ~1 frame ahead of the coro/recomp path — introduced twice (+1 at DEMO leave, +1 at GAME cutscene entry); durable `PSXPORT_SBS_STAGETRACE=1` diagnostic landed [PARTIALLY CLOSED — Slip #2 fixed 31fa879; Slip #1 residual remains, tracked below]** [?] —   ↪ docs/findings/sbs.md
- **FULL-mode SBS filters — arena offset + counter noise-filtered; next real byte is `0x800ED098` (off by 3, likely object count) [SUPERSEDED — this block was written before the readers were grep'd; the NOT-noise-filter finding is above]** [?] —   ↪ docs/findings/sbs.md
- **FULL-mode SBS — first byte 0x800BF878 is a per-frame counter off-by-1 (timing); volume driver is the 0x800F0000 object-pool base-address divergence (~3.2K bytes) [FILTERED 2026-07-03]** [?] —   ↪ docs/findings/sbs.md
- **FULL-mode next gap: `0x800ED098` = pool free-slot count, native leads PSX by 3 slots — REAL port gap (gameplay reads it against literals `< 2`, `< 4`, `< 10`, `< 13`) [PROXIMATE CAUSE ATTRIBUTED — see block above]** [?] —   ↪ docs/findings/sbs.md
- **FUN_801337E4 RE'd semantic model (2026-07-03) — full 5-way sub-state machine named; port-ready** [?] —   ↪ docs/findings/sbs.md
- **Faithful GAME-stage fiber + ra sweep (2026-07-07, 4f0afba) — autonav frontier f28 = native submode0 vs guest** [?] —   ↪ docs/findings/sbs.md
- **Faithful IDLE LOOP zero-diff 137k+ frames (2026-07-07, 2acc712) — frontier now GAME-stage entry (autonav f26)** [?] —   ↪ docs/findings/sbs.md
- **Faithful SOP intro byte-exact (2026-07-07, c4c27a3) — frontier f114 packet-pool pointer** [?] —   ↪ docs/findings/sbs.md
- **Faithful frontier f2 -> f11 (2026-07-07 session 2) — task-1 + DEMO on native fibers; LAST-WRITER map lands** [?] —   ↪ docs/findings/sbs.md
- **Faithful frontier: f117 -> 32k+ zero-diff; skip observable gate passes the area load (2026-07-08)** [?] —   ↪ docs/findings/sbs.md
- **Faithful s48_2 chain discipline (2026-07-07, 640d23e) — frontier f29 = SOP field-mode** [?] —   ↪ docs/findings/sbs.md
- **Full-PSX (SBS core-B / psx_fallback) FREEZES after Start — recompiler SPLITS the GAME stage fn (prologue doesn't flow into its loop)** [FIXED (later-269) — recompiler emits a fall-through tail-call at deliberate re-entry seeds.] — full-PSX path (PSXPORT_SBS_MODE=both core B, or PSXPORT_SELFTEST=startgame) — mashing Start at the title DOES start the game (reaches GAME stage 0x8010637C) but the field then FREEZES: the GAME field state machine never advances (sm[0x48] stuck at 0, never reaches free-roam 2). sched log shows `slot 0 coro start st=3 entry=0x8010637C` exactly ONCE, then nothing — the coro is `done=1` after one resume with no `ov_switch` yield.  ↪ docs/findings/sbs.md
- **Full-PSX SBS modes (gameplay/both) fail-fast at a scheduler yield-return (post-interpreter)** [fixed (later-264) — full-PSX runs recompiler-only via thread-fiber coroutines + load-time overlay-identity routing; both cores boot START→DEMO→GAME with 0 recomp-MISS, no crash/hang.] — `PSXPORT_SBS_MODE=both` (or `=gameplay`) aborts at frame 0 with `[recomp-MISS 0] no recompiled fn for 0x80051FA4 (caller ra=0x80051FA4, a0=0xFF000000)`. `mode=render` is fine (runs indefinitely).  ↪ docs/findings/sbs.md
- **Full-PSX field-mode does NOT fully run under coroutines past the intro cutscene (deeper limit)** [known-issue (full-PSX coroutine-resume limit; the SHIPPING game is the NATIVE path, which runs the field fine — 4000 frames clean)] — after the prologue-split + SPU-advance fixes, the full-PSX (psx_fallback / SBS core B) field reaches free-roam, runs its loop, plays + completes the intro XA clip — but then the GAME outer loop counter *(0x1F800198) stops advancing (stuck ~34); the coro keeps yielding but the field-mode (sm[0x48]=2, sm[0x4a]=0 submode0, sm[0x4e]=1 → overlay 0x80109450) doesn't progress.  ↪ docs/findings/sbs.md
- **HOLLOW GATE: free-roam SBS never runs native field-frame code (fieldFrameX / updateTail) (2026-07-11)** [?] —   ↪ docs/findings/sbs.md
- **Headless audio-gated game LOGIC hangs — SPU/XA stream only advanced when an output consumer exists** [fixed (later-270)] — headless (no audio device, no WAV) — a field/cutscene that starts an XA voice/BGM clip and waits `while(*(0x801fe0e0)!=0)` for it to finish NEVER advances; the clip's read head never moves so the wait can't clear. Looks like a freeze ("frozen, music would be playing").  ↪ docs/findings/sbs.md
- **Historical (2026-07-04) — Slips #6/#7/#8/#9 all obsoleted by the fiber-only design** [?] —   ↪ docs/findings/sbs.md
- **Historical Slips (pre-2026-07-04 — obsoleted by the fiber design above)** [?] —   ↪ docs/findings/sbs.md
- **Job #1 replay sweep — pc_faithful byte-exact across the full replay library (2026-07-15)** [?] —   ↪ docs/findings/sbs.md
- **LIVE-REGISTER LAW: callees spill the caller's caller-saved regs — mirror the full register file at every dispatch boundary (2026-07-16)** [?] —   ↪ docs/findings/sbs.md
- **MODE=skip f465 area-FX divergence — RESOLVED (2026-07-15): rendezvous on 0x1F80019A** [?] —   ↪ docs/findings/sbs.md
- **Native gameplay is nearly deterministic — first real port gap is `0x800BF81E` latch at f217 (SUPERSEDED)** [IDENTIFIED, ROOT CAUSE PARTIALLY NAMED. The seed of divergence is `game/scene/engine_stage.cpp:1233` — `if (c->mem_r8(0x800BF9C3u) & 0x80u) c->mem_w8(0x800BF81Eu, 1);`. Predicate `0x800BF9C3 & 0x80` evaluates differently between core A and core B at f217. Writer of `0x800BF9C3` is unknown from decomp grep alone (`scratch/decomp/ram_f1000_all.c` shows only the READ at line 36997 — the write goes through some struct-relative store Ghidra didn't associate as a DAT_ symbol). Since both RENDER cores run identical native gameplay, the differing predicate must trace back either to the 0x801F0000 scheduler-control page (timing-sensitive writer) or to a still-shared shared static missed by deglobalize Phases 17–24.] —   ↪ docs/findings/sbs.md
- **PC_FAITHFUL byte-exact IN GAMEPLAY (2026-07-04) — M_GAMEPLAY 22,980+ frames zero-diff** [?] —   ↪ docs/findings/sbs.md
- **PRIOR ATTRIBUTION (kept for the trail) — Per-ra bucketed ALLOCTRACE landed as a durable settled-state compare — `PSXPORT_SBS_ALLOCTRACE=1` now dumps at exit; +6 net traced to native-record_gate lump + one A-only recomp bucket ra=0x801298A4 (beh_anim_trigger_gates → FUN_8012982C init 4-alloc loop, 2 invocations)** [?] —   ↪ docs/findings/sbs.md
- **PRIOR FINDING (kept for trail; falsified by the block above) — 3-slot 0x800ED098 delta CALLER NAMED — native `beh_flagbit_timer_machine` (FUN_8013B2E4) fires on A but NOT on B; each STATE 0 entry runs FUN_800519E0(node, count=3, …) = 3 record allocs** [?] —   ↪ docs/findings/sbs.md
- **Panes ≠ standalone = SBS special-casing, NOT a cross-core leak (2026-07-10)** [?] —   ↪ docs/findings/sbs.md
- **Phase 5 exploration — composition works, all reachable scenes 0-diff (2026-07-15)** [?] —   ↪ docs/findings/sbs.md
- **Phase 5 — autonomous headless SCENE EXPLORATION under SBS: VALIDATED recipe (2026-07-15)** [?] —   ↪ docs/findings/sbs.md
- **Port-review bug taxonomy — three named classes of native-transcription defect (2026-07-03)** [?] —   ↪ docs/findings/sbs.md
- **Post-Slip-#1 wide BYTETRACE classification (2026-07-03) — most of remaining 1880 B ramdiff is low-signal; two actionable clusters named** [?] —   ↪ docs/findings/sbs.md
- **Post-fix leading-edge f217 divergence (0x800EE0DD..0x800EE10C) is a SCHEDULING-PHASE residual, NOT another case-swap — value-set/count identical A vs B, only phase differs** [?] —   ↪ docs/findings/sbs.md
- **Rewind-and-arm is UNSOUND with live fibers — skipped; sbs diff shows detection-time bytes (2026-07-07)** [?] —   ↪ docs/findings/sbs.md
- **SBS = strict pc_faithful, hard-wired (2026-07-07, ba4f50b) + FUTURE: pc_skip observable-output compare** [?] —   ↪ docs/findings/sbs.md
- **SBS DEMO attract-path crash — hits an unrecompiled overlay function 0x80109F7C** [OPEN, LOW PRIORITY (dodged by the normal SBS use path — `PSXPORT_SBS_AUTONAV=1` drives out of DEMO into GAME before the attract timer fires). Not the "both panes look weird" bug.] — with SBS running headless and no autonav (`PSXPORT_SBS=1 PSXPORT_SBS_MODE=<any>`, no `PSXPORT_SBS_AUTONAV=1`), letting DEMO idle to its attract timeout aborts core A with `rec-MISS 0x80109F7C` at `demo_frame_s7` → `ov_demo_gen_80106E28` → `gen_func_80059D28` → `gen_func_8005950C` → `gen_func_80058648` → `rec_dispatch_miss`. Standalone at 1500 frames of DEMO idle never enters the attract path (`sm[0x48]` stays at 2), so this is SBS-scoped.  ↪ docs/findings/sbs.md
- **SBS both cores freeze after Start — diff_mode skips the SPU/XA advance → intro-cutscene XA clip wait never clears** [PARTIAL — XA clip wait FIXED (later-271); a DEEPER recomp-coro register-corruption freeze remains (next block).] — `PSXPORT_SBS_MODE=both` — after reaching the field, BOTH panes freeze: core A (native) renders the field but static, core B (full-PSX) black/stuck ("everything frozen, music keeps playing"). Headless: core B `0x801fe0e0`(task-2 state)=2 forever, GAME loop counter `0x1f800198` stuck ~34. The leading "SBS loop hangs on core B's `step_core`/`co->resume()`" hypothesis is FALSE — the lockstep loop iterates fine (core A advances to counter 3532 in autonav); core B is genuinely stuck at a wait, still yielding each frame.  ↪ docs/findings/sbs.md
- **SBS core-B (full-PSX recomp) FREEZES at the title — dead libetc VSync counter (timebase)** [PARTIAL — vblank counter fixed (later-268, commit 106dcda); menu-state divergence still UNCONFIRMED/open] — `PSXPORT_SBS_MODE=both` — core B (full PSX) sits frozen at the Tomba!2 title; core A (native) does not. sched log loops `slot 0 coro resume 0x801062E4` + `slot 1 coro start 0x80044F58` forever, never transitioning.  ↪ docs/findings/sbs.md
- **SBS core-B (full-PSX) field FREEZES deeper — recompiler mis-emitted FUN_8003c048's in-function jump-table `jr` (skips epilogue → corrupts the GAME-loop's callee-saved s0/SP)** [FIXED (later-272) — recompiler jump-table recovery + cross-boundary switch-target seeding. Core B's GAME loop counter now advances (3791→3953 over 10s; was stuck 34); selftest asserts sustained field progress.] — after the later-271 XA fix clears the clip wait, core B's field STILL freezes (genuinely stuck: ~4 bytes of main RAM change over 30s, all housekeeping counters; scratchpad 0-diff; `0x1f800198` stuck at 34; field SM `0x801fe048` byte-identical over time). The SHIPPING NATIVE core (A) runs the field fine.  ↪ docs/findings/sbs.md
- **SBS crashes on rec_dispatch_miss(0x80109F7C) during DEMO attract cursor=1 (A01)** [RE'd — root cause identified; fix not landed (needs game-content RE to know the correct handler for A01).] —   ↪ docs/findings/sbs.md
- **SBS gameplay-mode "both sides look identical / no divergence" — SUPERSEDED by the block above** [?] —   ↪ docs/findings/sbs.md
- **SBS gameplay-mode "both sides look identical / no divergence" — investigation, PARTIAL step, root cause NOT yet pinned** [OPEN. One necessary widening applied (below). Sufficiency NOT verified.] — `PSXPORT_SBS_MODE=gameplay` shows the two panes as visually identical, and no divergence surfaces on the debug server, even in cases where native gameplay demonstrably perturbs behavior (e.g. wrong SFX under native, correct under `PSXPORT_GATE=1`). USER read (2026-07-02): "both cores run the same code, and same renderer."  ↪ docs/findings/sbs.md
- **SBS gameplay-mode panes look "impossibly different" — not a harness bug, that IS the gameplay divergence** [RESOLVED — SBS is doing exactly what it's designed to do. The reason no standalone combo reproduces the A pane visually: standalone with `PSXPORT_RENDER_PSX=1` renders solid black at f400 (its own separate plumbing gap), so there is no standalone route to "native gp state + PSX render pipeline + composite present" that SBS's per-pane readback provides. The A pane is a first-hand look at the wrong-SFX bug's guest-state footprint rendered through PSX.] —   ↪ docs/findings/sbs.md
- **SBS pane A = the USER's real config (2026-07-10, follow-up 2)** [?] —   ↪ docs/findings/sbs.md
- **SBS pane parity vs standalone configs — VERIFIED pixel-faithful (2026-07-10, follow-up)** [FIXED + mechanically verified.] —   ↪ docs/findings/sbs.md
- **SBS panes render BLACK for the field (both panes all-black / region-nonzero=0)** [fixed (render_queue.cpp flush diff_mode gate)] — `PSXPORT_SBS_MODE=both` dump at free-roam is all black; `PSXPORT_GPU_TRACE=1` readback shows `region-nonzero=0/76800 ... batch tri=0 tex=0 worldquads=1109` — the native core EMITS 1109 world quads but the geometry batch is empty at `grab_pane`.  ↪ docs/findings/sbs.md
- **SBS panes ≠ standalone configs — process-global enhancement/render state (FIXED 2026-07-10)** [FIXED (structural deglobalization, 3 layers).] —   ↪ docs/findings/sbs.md
- **SBS self-surfacing sweep 2026-07-15 — pc_faithful CLEAN; pc_skip audio-init fork diverges** [?] —   ↪ docs/findings/sbs.md
- **SBS skips gpu_present_ex → per-frame render bookkeeping never resets → wrong ordering (2026-07-10)** [?] —   ↪ docs/findings/sbs.md
- **SBS tooling: wwatch fire now prints guest + host backtrace inline** [?] —   ↪ docs/findings/sbs.md
- **SKIP-mode frame alignment (USER 2026-07-10) — mechanism landed, first fork wired, honest frontier** [?] —   ↪ docs/findings/sbs.md
- **Slip #1 residual CLOSED — defer native DEMO substate 5 (LEAVE→GAME) by one tick to match coro yield cost (2026-07-03, 550178a)** [?] —   ↪ docs/findings/sbs.md
- **Slip #3 CLOSED — submode1 case 0 collapsed FUN_80044BD4's yield; two-tick split matches coro cadence (2026-07-03)** [?] —   ↪ docs/findings/sbs.md
- **Slip #5 (OPEN) — massive RNG cadence skew: guest LCG at 0x80105EE8 runs 158× more advances on B than A (7411 vs 47) → downstream spawn coord divergences (2026-07-03)** [?] —   ↪ docs/findings/sbs.md
- **Slip #6 (ORIGINAL OPEN NOTE, kept for the trail) — startBinStage f0 stack scratch: FUN_80044BD4 wait-loop yields ~48× on B while native task-0 doesn't yield at all (2026-07-04)** [?] —   ↪ docs/findings/sbs.md
- **Slip #6 CLOSED (2026-07-04, commit 656bd2b) — three-layer stack-scratch byte-match for FUN_80044BD4 + subframe + outer-yield emission** [?] —   ↪ docs/findings/sbs.md
- **Slip #7 (OPEN) — libgs DMA-state divergence at 0x800AC61C — call ordering between A's native startBinStage LoadImage dispatch and B's task-1 fiber preload chain (2026-07-04)** [?] —   ↪ docs/findings/sbs.md
- **Strict mirror TDD gate landed (2026-07-08) — past "verified" claims are UNVERIFIED** [?] —   ↪ docs/findings/sbs.md
- **Target #2 (stride-0x88 rate mismatch) is NATIVE-RENDER-vs-PSX-RENDER, NOT a case-swap — DO NOT chase it with the target-#1 primitive** [?] —   ↪ docs/findings/sbs.md
- **Target #4 (stride-0xC4 pool at 0x800F00xx) FALSIFIED as a beh_typed_table_seed_gate transcription defect — it's DOWNSTREAM propagation, not a case-swap (2026-07-03)** [?] —   ↪ docs/findings/sbs.md
- **Target #4 upstream probe (2026-07-03) — `PSXPORT_SBS_UPPROBE=<addr>` landed; obj = 0x800EFFF4 named; obj+0x42 OVERLAPS rec+0x0C (KEY LAYOUT INSIGHT); divergence proven at FUN_80083F50-arg level but source not yet named (many callers pollute PSXPORT_DISPWATCH)** [?] —   ↪ docs/findings/sbs.md
- **`0x800BF81E` RENDER-mode divergence — writer is `DualviewSnapshot::restorePre` bulk memcpy rewinding `submitPage810c`'s gameplay-relevant writes (native-render decoupling by design) [FIXED 2026-07-03 — b9ccd2d]** [ROOT CAUSE CONFIRMED. Not a `mem_w8` writer at all — a `memcpy(c->ram, snapshot_ram, 0x200000)` in `DualviewSnapshot::restorePre` (runtime/recomp/dualview_snapshot.cpp:18) called from `Engine::fieldFrame` (game/scene/engine_stage.cpp:428). The flow inside fieldFrame:] —   ↪ docs/findings/sbs.md
- **`PSXPORT_DISPWATCH=<addr>:ra=<caller_ra>` — caller-ra-scoped dispatch trace (2026-07-03, workflow-first tool extension)** [?] —   ↪ docs/findings/sbs.md
- **`PSXPORT_SBS_BYTETRACE=<lo>,<hi>` landed — per-byte-value+ra bucketing with settled-state 4-class auto-classifier + suggested SBS-noise-filter ranges + top-RA on REAL rows** [?] —   ↪ docs/findings/sbs.md
- **f0 strict-faithful divergence ANATOMY (2026-07-07, historical — see CLOSED entry above) — dead MIPS stack scratch vs live yield context; reorg verified non-regressive** [?] —   ↪ docs/findings/sbs.md
- **f0 strict-faithful divergence CLOSED by the faithful-execution model (2026-07-07); frontier now f2 FUN_800754F4 yield cadence** [?] —   ↪ docs/findings/sbs.md
- **oraclediff: FREE-ROAM GAME STATE is also convergent — the native opening gameplay matches the PSX oracle** [VERIFIED CONVERGENT (later-282) — native free-roam onset state == PSX oracle, only benign drift.] — after fixing the narration render bugs, need to confirm the ACTUAL playable game (free-roam field) is correct native-vs-PSX, not just the scripted cutscene.  ↪ docs/findings/sbs.md
- **oraclediff: interactive-play SCAN added (harness); Tomba NOT yet controllable at onset; frontier was STALE** [HARNESS added (later-283); interactive-MOVEMENT convergence NOT yet verified (Tomba not controllable at the tested checkpoint — first commit OVERCLAIMED it, corrected same session). Also CORRECTED the stale ov_terrain-orphaned claim.] — need to know if the native port matches the PSX oracle during INTERACTIVE play (Tomba under player control), not just the scripted opening; and the frontier said "wire the orphaned PC-native ov_terrain into the field terrain path".  ↪ docs/findings/sbs.md
- **oraclediff: narration GAME STATE is convergent native-vs-PSX — cutscene bugs are RENDER-only** [known-issue (RAM diff exhausted; bugs are render-only — needs the Phase-2 software-GPU/VRAM diff, docs/oracle.md)] — intro-narration render bugs (void scene draws sea / missing effect, cliff water corrupt); oraclediff (PSXPORT_SELFTEST=oraclediff) showed a huge ~615-byte all-zero-vs-populated divergence at 0x80102xxx that looked like missing scene-setup state  ↪ docs/findings/sbs.md
- **recomp-MISS 0x80146478 in narration cutscene — PSX core never loads the A00 AREA CODE overlay** [FIXED (later-274) — the 0x80146478 miss no longer occurs; the narration now advances to a SEPARATE, later miss `0x8010BF54` (a SOP-overlay routing issue, tracked below). This is a SHIPPING-game bug, NOT SBS-specific: the native single-core game (`PSXPORT_REPL=1`, `newgame` then `run 2500` with NO Start) reproduced the same miss. AUTO_SKIP hid it by SKIPPING the intro (pressing Start ends the narration before the offending render runs).] — `PSXPORT_SBS_MODE=both` (windowed user repro: Start ×2 in the main menu → narration cutscene → "freezes" a few seconds in). Process aborts with `[recomp-MISS 0] no recompiled fn for 0x80146478 (caller ra=0x8003F6F0, a0=0x8017C858)`. The new miss diagnostic prints `resident overlay for this slot = SOP` and `[miss-state] stage=0x8010637C sm[0x50]=2 1f80019b=1` (load marked DONE).  ↪ docs/findings/sbs.md
- **spawn-leaf frame residual @0x801FE918 — natural free-roam divergence (2026-07-11, RESOLVED 2026-07-14)** [?] — SBS-full, normal AUTO-NAV area-0 free-roam (no forcing), diverged @f491 at  ↪ docs/findings/sbs.md
- **walkStart early-exit spill gap @0x801FE8CC — watch-cut f747 divergence (2026-07-16, RESOLVED same day)** [?] —   ↪ docs/findings/sbs.md

## scene

- **Cross-area interior code-overlay never loads — RE chain to the sole MODE-slot loader (2026-07-17, OPEN, RE-frontier)** [?] —   ↪ docs/findings/scene.md
- **Cutscene SCRIPT INTERPRETER — resident dispatch loop + entry format + handler table (2026-07-03)** [RE'd (structure fully identified); NATIVE PORT NOT YET LANDED (opcode inventory of ~63 handlers is a bounded follow-up).] — cutscene fade fn pointers (0x8013B29C, 0x8013B074, 0x8013B178, 0x80139728, 0x8013B274, 0x8013AEF0, 0x8013AFD8) live only as DATA WORDS in A06 script tables (e.g. 0x80149A20, 0x80149CDC), never as direct-jal targets. When investigating "who calls FUN_80139728" or trying to intercept the fade fnptr from a native override, greps return zero jal sites — you need to know it's dispatched through the script interpreter.  ↪ docs/findings/scene.md
- **Door/area-transition freeze: FUN_80073328 case 3 stalls; native AND substrate both hang here on REAL gameplay input** [ROOT CAUSE OPEN — the prior "repro artefact of teleport" claim (2026-07-02 morning) is FALSIFIED by this pad-replay: the freeze reproduces from PURE walking input (no `w` writes), so it IS a real gameplay bug the port ships with, hitting BOTH the PC-native path AND the PSXPORT_GATE=1 substrate. Something upstream (present in both) fails to advance the transition.] — deterministic pad-replay repro (2026-07-02, HEAD 4ad1119) — no teleport: `PSXPORT_AUTO_SKIP=1` reaches free-roam at f216 (spawn ~(3940,-1032,3968)), then walk right 34f + hold up 300f puts Tomba at (4675,-1023,4608) with `sm[0x48]=2 4a=1 4c=3 4e=1, bf818=2` STUCK forever (verified stable through f1234 = 600 idle frames after the walk). Same freeze under PSXPORT_GATE=1 substrate. Deterministic replay: `PSXPORT_PAD_REPLAY=scratch/bin/door_freeze.pad` reproduces bit-perfect. Dumps: `scratch/bin/door_freeze_f{634,934}.bin(+.spad)`.  ↪ docs/findings/scene.md
- **FUN_80040B48 dual-ownership (scene-event ARM) — DEDUPED (2026-07-15)** [?] —   ↪ docs/findings/scene.md
- **FUN_80040CDC dual-ownership (script init mis-named "animEnvInit") — DEDUPED (2026-07-15)** [?] —   ↪ docs/findings/scene.md
- **Narration cutscene out of sync in MODE=skip: DEMO→GAME handoff 12 frames early on the skip pane (2026-07-14, RESOLVED)** [?] —   ↪ docs/findings/scene.md
- **Prologue audio + anim divergences: one-byte instrument slip, stale-a3 anim seed, missing guest-frame mirrors (2026-07-10, RESOLVED except font)** [?] —   ↪ docs/findings/scene.md
- **Prologue-vortex root cause #2: SOP op3E wrappers discarded v0 (2026-07-10, ultracode workflow, RESOLVED)** [?] —   ↪ docs/findings/scene.md
- **Prologue-vortex root cause #3: objMatrixCompose dispatched FUN_80051128 with stale a0 → matMul zeroed SOP script data (2026-07-10, RESOLVED)** [?] —   ↪ docs/findings/scene.md
- **Remaining codemap --conflicts candidates (2026-07-15) — TO TRIAGE next iteration** [?] —   ↪ docs/findings/scene.md
- **Resident-aware override dispatch — REJECTED (residency gate breaks render path) [2026-07-17]** [?] — batch-owning 291 "a00-unique" leaves via `ov_a00_set_override` → boot SIGABRT (both pc_skip + SBS), 0 leaves fired before abort. 190 MAIN-range batch is fine.  ↪ docs/findings/scene.md
- **SOP intro-cutscene cluster (0x8010AF60-0x8010BEAC) + Demo::s3SubMachine (0x80106AC4) — §9 promote pass (2026-07-10)** [SOP cluster now 6/6 promoted VERIFIED+WIRED (sopBeatAdvanceWalk 0x8010AF60,] —   ↪ docs/findings/scene.md
- **ScriptInterp opcode cluster — §9 re-verify + frontier-tier wiring (2026-07-10)** [RESOLVED (5 opcode handlers + advanceStep + PcScheduler::tickSleepCountdown promoted] —   ↪ docs/findings/scene.md
- **Un-owned entity-behavior cluster (0x801244E8/0x8012866C/0x8012E168/0x8013DD48) — 1 ported, 3 blocked** [?] —   ↪ docs/findings/scene.md
- **Vortex void beat black on pc_skip — RESOLVED (2026-07-14, bug #43)** [?] — default config (pc_skip + pc_render), `newgame; run 600`: black + narration text  ↪ docs/findings/scene.md
- **pc_skip FUN_80044BD4-collapse INCOMPLETENESS class (2026-07-15) — audit of the #53 bug family — FIXED** [?] —   ↪ docs/findings/scene.md
- **pc_skip exec: prologue vortex backdrop missing + scene SM stalls before area load (2026-07-10, RESOLVED)** [?] —   ↪ docs/findings/scene.md

## tooling

- **A debug-server client disconnect kills the whole game (SIGPIPE)** [fixed 10a07e0] — the live port dies ("connection refused" on the next dbgclient call) when a debug client is killed / times out mid-reply — looked like a crash on entering a scene, but no abort/diagnostic is printed.  ↪ docs/findings/tooling.md
- **A fully-RE'd, bit-exact `class Math` (GTE matrix cluster) sat dead — nobody ever called `registerOverrides()`** [fixed (this session) — `Math::registerOverrides()` added + called from `boot.cpp`; SBS] — `tools/codemap.py --addr 0x80084110` said "NO native owner found", but `Math::matMul` was  ↪ docs/findings/tooling.md
- **CRITICAL (found + fixed 2026-07-08): SBS/DualCore/Selftest never populated their own Game's EngineOverrides table — every EngineOverrides-only override was COMPLETELY INERT under SBS** [fixed (commit b078729, merged to main). New `ovhit` channel added alongside so this] — a verification pass (spawned to prove EngineOverrides actually intercepts on the  ↪ docs/findings/tooling.md
- **Engine/game overrides on the process-global g_override[]/g_ov_* tables contaminated the SBS oracle (false 0-div)** [fixed 2026-07-09 (central oracle-gated thunk installer).] — SBS full reported 0 `sbs-div` through 15000+ frames for clusters that were actually WRONG. The oracle (core B) was running the NATIVE mirror for several render-packet-emitter clusters, not the pure substrate — so SBS compared native-vs-native and reported a clean gate, masking every real divergence.  ↪ docs/findings/tooling.md
- **EngineOverrides::register_ is BLIND to a direct substrate call — needs shard_set_override too** [documented workaround (this session) — a real fix would make `EngineOverrides::register_`] — a new native port (ActorReward, FUN_80049A60/9E54/A3D4/B150/B208) registered ONLY via  ↪ docs/findings/tooling.md
- **ORPHAN native with a `_verify` wrapper is NOT proof it's byte-perfect** [workflow caveat (2026-07-02)] — trying to wire ov_orch597AC (FUN_800597AC) at engine_submit.cpp:1211 tripped a 9-byte main-RAM A/B mismatch @0x1FFCC9 at frame 500 even though the native has an `orchverify` gate. Wire had to be reverted.  ↪ docs/findings/tooling.md
- **Reaching the New-Game cutscene live: mash START (it confirms the menu AND skips FMV)** [known-good (how-to)] — automated drive (debug-server taps) loops in the attract demo / never reaches the field cutscene (stage 0x8010637C); the title menu's confirm wasn't registering.  ↪ docs/findings/tooling.md
- **SBS both-mode: panes black + silent on macOS/MoltenVK (fine on linux/AMD)** [ROOT-CAUSED + fixed (barrier READ access) — awaiting Mac confirm] — `PSXPORT_SBS_MODE=both ./run.sh` reaches free-roam but BOTH panes are black at EVERY stage (title/demo AND field — confirmed by the user it's total black) and there's no audio, on Apple M2 / MoltenVK; only the RmlUi overlay shows. Plain `./run.sh` (single core) is visible AND audible on the same Mac.  ↪ docs/findings/tooling.md
- **Wiring a banked draft found 2 real transcription bugs via generated/ byte-trace (not caught by SBS alone)** [?] — two wide-RE-drafted-but-unwired clusters (game/object/cube_text_ledger.{h,cpp},  ↪ docs/findings/tooling.md
- **codemap.py mis-parsed inline description addresses as owner tags** [fixed 7a396b4 (2026-07-02, session 15)] — `tools/codemap.py --addr <hex>` reported false owners for many addresses. E.g. `0x8003E264 → ov_transition_e20 [LIVE]` was wrong: ov_transition_e20's real guest address is 0x80107E20, and 0x8003E264 is just a description reference (`// FUN_80107e20 — … 1 effect 0x8003e264 …`). Trap: acting on that mapping would try to "wire" a substrate address as if it were already native.  ↪ docs/findings/tooling.md
- **codemap.py: an orphan doc-comment block hides the addresses of the def below it** [worked-around (blank-line separation); underlying heuristic gap noted] — after owning a function natively (correct code, builds, A/B 0-diff), `tools/codemap.py --addr 0x80025588` still prints "NO native owner found", and docs/code-map.md maps the new native symbol to a WRONG/neighboring guest address (with a mismatched description).  ↪ docs/findings/tooling.md
- **ovhit A/B mismatch is often a call-GRAPH asymmetry, not a counting bug** [RESOLVED — every one triaged is a measurement ARTIFACT, not a functional defect.] — the first honest `PSXPORT_DEBUG=ovhit` SBS-full run (post the 2026-07-10 target-  ↪ docs/findings/tooling.md
- **recdep / any rec_dispatch hook is BLIND to resident→resident (intra-MAIN) calls** [known-issue / measurement caveat (2026-07-01, later-293)] — `recdep` and ad-hoc `camtrace`-style hooks in `rec_dispatch` show ZERO calls to a resident MAIN function (e.g. the camera 0x8006E3B0/0x8006D02C) even while it demonstrably runs every frame — leading to a WRONG "that code never runs here" conclusion (cost later-290..292 chasing a phantom free-roam camera).  ↪ docs/findings/tooling.md
- **recdep hot-leaf leaderboard: entries may ALREADY be native-ported (ORPHAN) — check codemap FIRST** [workflow rule (2026-07-02)] — the handoff points at the top substrate-dispatch entries by calls/frame (e.g. 0x8007778C @115k, 0x80077ACC @48k, 0x8002B278 @38k, 0x8004D7EC @36k) as "port these next". Session-2026-07-02 disassembled 0x8007778C to reimplement — only to discover `tools/codemap.py --addr 0x8007778C` already shows it as `ov_cull_wrapper` in game/render/cull.cpp, ORPHAN. Same for the other three. Wasted time on a redundant RE pass before the check.  ↪ docs/findings/tooling.md

## ui

- **DRAFTED (UNWIRED/UNVERIFIED): dialog/text-box byte-stream advance — 0x8007C0D0 + 0x8007D0D0 (2026-07-08)** [?] —   ↪ docs/findings/ui.md
- **Dialog text-box PANEL emitter chain — RE'd (2026-07-14, for bug #34)** [?] —   ↪ docs/findings/ui.md
- **MAPPED (not drafted): pause/quit-menu + options-screen widget builders — 0x8007EAE4-0x8007FDB0** [?] —   ↪ docs/findings/ui.md
