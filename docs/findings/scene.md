# Findings — scene/area transitions

## Door/area-transition freeze reproduces UNDER PSXPORT_GATE=1 → NOT a native-only regression
- **symptom:** headless repro of the "seaside village X≈14656 door" freeze (handoff_door_freeze.md): teleport Tomba master-pos X=0x39400000 (16.16), tap `right`, run ~900 frames. `sm[0x4c]` flips 2→3 at frame ~877 and stays 3 forever (`sm[0x48]=2 4a=1 4c=3 4e=1`). Same freeze on both PC-native (default) AND PSX-substrate (`PSXPORT_GATE=1`) gameplay paths (`sm[0x4c]==3` at frame 890 in both), verified 2026-07-02 on HEAD `169dff7`.
- **status:** DIAGNOSED — the falsified hypothesis is in the previous handoff; the actual likely cause is the repro itself.
- **falsified hypothesis (handoff_door_freeze.md):** the "destination-area cooperative task is orphaned post-override-removal on the NATIVE path" reading was wrong. Under `PSXPORT_GATE=1` the whole-frame behavior runs via rec_dispatch through the recomp substrate (no native scheduler in the loop), and the freeze still occurs — so it is not a native-scheduler bug.
- **likely cause (needs repro-side confirmation):** the repro pokes Tomba's master-pos X directly (`w 800e7eac 39400000`) without going through the game's warp/area-select machinery. The trigger fires (bf818 progresses 0→1→2, sm[0x4c] 2→3, `FUN_80026ad0` case 1 tears down and jumps into case 6 → 2 → 3) but the destination-area-id / next-scene handshake that would drive `FUN_80073328` case 3 → `FUN_80073300` (which writes bf818=3) never runs, because the game's warp path never selected a destination. So `FUN_80026ad0` case 3 waits on bf818==3 forever. This is a REPRO artifact, not a game bug — real players walk there and the warp trigger populates the destination properly.
- **fix (proper repro):** use the pad recorder. `PSXPORT_PAD_RECORD=scratch/bin/hut.pad` on a hand-driven windowed run that walks Tomba into the house (no teleport), then replay headless with `PSXPORT_PAD_REPLAY=scratch/bin/hut.pad` (byte-perfect deterministic — verified 2026-07-02: record-vs-replay RAM cmp = 0 @ frame 400). Only then compare native vs PSXPORT_GATE=1 to see whether there IS still a native-only bug.
- **FUN_80073328 host node:** driven by behavior-handler 0x80139274 (overlay, `beh_typed_jumptable_pair.cpp` JT1 cases 0/1/2 all `rec_dispatch(0x80073328)`), NOT by the trigger `FUN_800739AC` (0x800739ac). During `sm[0x4c]==3` the entity walk `0x8007b04c` gates on `node+0x28 & 0x80` — whether that flag holds on the 0x80139274 host is unknown; check on a pad-recorded genuine transition.
- **already owned along the chain:** `FUN_80108A60`/`ov_field_transition` (state-3 dispatcher, engine_stage.cpp:893), `FUN_80044BD4`/`native_area_load_bd4` (0x800452C0 area-load body, engine_stage.cpp:729), `FUN_80106b98`/`ov_field_run`, `FUN_8002655C`/`ov_bg_scene_transition_sm` (SOP intro fade), the beh_typed_jumptable_pair handler. NOT owned: `FUN_800782F0` (small flag-setter — sets bf870/bfe50/bf9db, not the transition entry it's labeled in older notes), `FUN_80073300` (bf818:=3), `FUN_80073328` (transition sequencer), `FUN_800732C0` (bf818:=1 kick).
- **refs:** scratch/handoff_door_freeze.md (superseded hypothesis), scratch/handoff_hut_fade.md, engine_stage.cpp (ov_field_transition / native_area_load_bd4), game/ai/beh_typed_jumptable_pair.cpp (0x80073328 host), scratch/decomp/ram_f1000_all.c:52186-52316 (FUN_80073260/2c0/300/328 decomp), scratch/decomp/ram_f1000_all.c:6223-6291 (FUN_80026ad0 decomp)
