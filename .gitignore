# Generated recompiler output — regenerable from the ROM
generated

# Build dirs
build/
build-*/
build2/

# Scratch / run artifacts (see CLAUDE.md — never /tmp; all tooling output goes here)
scratch/

# Auto-memory (Claude session-local; per-machine, never committed — see CLAUDE.md "Global working principles")
memory/
# stray manual frame-dump dir (PSXPORT_VK_SHOTSEQ qdir) — artifacts belong in scratch/
/x/

# Local config (ROM path etc.)
.env

# ROM / disc drop-ins (never commit copyrighted assets)
*.chd
*.bin
# the psxport fastboot OpenBIOS build is MIT and committed deliberately
!bios/openbios-fast.bin
*.cue
*.iso
*.img
*.exe
*.psexe
bios/scph5501.bin

# OpenBIOS source: reproducible build input (scripts/build-openbios.sh clones it); not vendored
vendor/openbios-src/

# recompiler generated output (regenerable; never hand-edit)
/generated
__pycache__/
*.pyc

# Ghidra-derived function list (optional local speed seed; never committed — keep repo public-clean)
tools/recomp/tomba2_funcs.txt
.claude/

# generated Vulkan SPIR-V header (tools/gen_vk_shaders.sh)
runtime/recomp/gpu_gpu_shaders.h
# generated SDL_GPU SPIR-V header (tools/gen_gpu_shaders.sh)
runtime/recomp/gpu_gpu_shaders.h

# standalone offline XA extractor binary (built by tools/build_xa_wavdump.sh)
tools/xa_wavdump
psxport_settings.ini
docs/reference/issues/shot_*.png
*.log
