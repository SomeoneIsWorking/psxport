#pragma once
// DemoLeafB — PORT_GEN scaffold class (see docs/port-framework.md). UNWIRED.
// Regenerate the body with: python3 tools/port_gen.py <addr> --class DemoLeafB --method run
class Core;

class DemoLeafB {
public:
  Core* mCore = nullptr;

  void run();
};
