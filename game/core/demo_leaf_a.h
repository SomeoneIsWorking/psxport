#pragma once
// DemoLeafA — PORT_GEN scaffold class (see docs/port-framework.md). UNWIRED.
// Regenerate the body with: python3 tools/port_gen.py <addr> --class DemoLeafA --method run
class Core;

class DemoLeafA {
public:
  Core* mCore = nullptr;

  void run();
};
